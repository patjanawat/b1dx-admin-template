# Enable MSW mock (1 = on, remove or set 0 to hit real BE)
NEXT_PUBLIC_MSW=0

# Core auth service base URL
# Dev:  https://localhost:7143
# Prod: https://api.yourdomain.com
CORE_API_BASE_URL=https://localhost:7143

# Allow gateway to proxy to localhost/private IPs
# Set to 1 for local dev only — must be unset or 0 in production
GATEWAY_ALLOW_PRIVATE_HOSTS=1
