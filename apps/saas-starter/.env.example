# ─── Mock (MSW) ──────────────────────────────────────────────────────────────
#
# NEXT_PUBLIC_MSW controls whether Mock Service Worker intercepts all API calls.
#
#   "1" → MSW ON  — ใช้ข้อมูล mock ทั้งหมด ไม่ยิงไป BE จริง
#                    ใช้สำหรับ local dev / demo เมื่อ BE ยังไม่พร้อม
#
#   "0" → MSW OFF — ยิงผ่าน gateway ไปยัง BE จริง (ต้องตั้ง CORE_API_BASE_URL)
#   unset         → เหมือน "0"
#
# Mock accounts (ใช้ได้เฉพาะตอน MSW=1):
#   username: admin   password: (อะไรก็ได้)
#   username: ops     password: (อะไรก็ได้)
#   username: viewer  password: (อะไรก็ได้)
#
NEXT_PUBLIC_MSW=0

# ─── Core API ────────────────────────────────────────────────────────────────
#
# Base URL ของ auth-service (.NET) ที่ gateway จะ proxy ไป
# ใช้งานเฉพาะตอน NEXT_PUBLIC_MSW=0
#
#   Dev:  https://localhost:7143
#   Prod: https://api.yourdomain.com
#
CORE_API_BASE_URL=https://localhost:7143

# ─── Gateway ─────────────────────────────────────────────────────────────────
#
# GATEWAY_ALLOW_PRIVATE_HOSTS อนุญาตให้ gateway proxy ไป localhost / private IP
#
#   "1" → เปิด  — ใช้สำหรับ local dev เท่านั้น
#   "0" → ปิด   — SSRF guard จะบล็อก private IP (ใช้ใน production)
#   unset       → เหมือน "0"
#
GATEWAY_ALLOW_PRIVATE_HOSTS=1
